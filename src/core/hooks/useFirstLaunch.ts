// src/core/hooks/useFirstLaunch.ts
// Re-export from context for backward compatibility.
// All existing imports of useFirstLaunch continue to work unchanged.
export { useFirstLaunch } from '../context/FirstLaunchContext';
